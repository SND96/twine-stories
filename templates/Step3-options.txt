if(choice=="replace the word recently with the date and touch nothing else")
              {
                document.getElementById("option1").click();
              }
elif(choice=="replace the word recently with the date and cite the source used")
              {
                document.getElementById("option2").click();
              }
elif(choice=="cite the source that can be used and let someone else replace recently")
              {
                document.getElementById("option3").click();
              }